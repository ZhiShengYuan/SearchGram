# SearchGram Engine - Environment Variables (Production)

# IMPORTANT: Copy this file to .env and fill in your actual values
# DO NOT commit .env to git!

# API Authentication (REQUIRED)
# Generate strong key: openssl rand -hex 32
API_KEY=your-strong-32-char-random-api-key-here

# Elasticsearch Password (REQUIRED)
# Generate strong password: openssl rand -base64 24
ELASTIC_PASSWORD=your-strong-elasticsearch-password-here

# Domain name (for SSL certificate)
DOMAIN_NAME=searchgram-api.yourdomain.com

# Email for Let's Encrypt (for SSL renewal notifications)
LETSENCRYPT_EMAIL=admin@yourdomain.com

# Cloudflare Tunnel Token (if using Cloudflare)
# CLOUDFLARE_TUNNEL_TOKEN=your-tunnel-token-here

# Optional: Admin API Key (different from regular API key)
# ADMIN_API_KEY=your-admin-api-key-here

# Optional: Allowed IPs (comma-separated)
# ALLOWED_IPS=1.2.3.4,5.6.7.8
